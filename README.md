# 🎪 RouteFest — Генератор идеального дня на фестивале

Веб-приложение для построения оптимальных маршрутов на фестивалях. Помогает выбрать события, избежать конфликтов по времени и создать идеальный план дня.

## 📋 Содержание

- [Возможности](#возможности)
- [Технологии](#технологии)
- [Требования](#требования)
- [Установка](#установка)
- [Запуск](#запуск)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Решение проблем](#решение-проблем)

## ✨ Возможности

### 🎯 Три сценария планирования:

1. **Индивидуальное планирование**
   - Выберите интересующие события
   - Система автоматически сгенерирует несколько вариантов маршрутов
   - Учет времени на перемещение между локациями

2. **Групповое планирование**
   - Добавьте участников группы
   - Каждый участник выбирает свои предпочтения
   - Поиск общих событий для всей группы

3. **Корректировка на лету**
   - Создайте начальный маршрут
   - Добавляйте новые события в реальном времени
   - Автоматическое перестроение маршрута

### 🔍 Удобные инструменты:

- **Поиск по названию** — быстрый поиск среди всех событий
- **Фильтры по времени** — утро, день, вечер
- **Подсветка ближайших событий** — визуальное выделение событий, которые скоро начнутся
- **Счетчик выбранных событий** — всегда видно, сколько событий выбрано
- **Экспорт маршрута** — копирование маршрута в буфер обмена для отправки друзьям

## 🛠 Технологии

### Backend:
- **Python 3.7+**
- **Flask** — веб-фреймворк
- **Flask-CORS** — обработка CORS-запросов
- **SQLite** — база данных

### Frontend:
- **Vanilla JavaScript** — без фреймворков
- **HTML5** — разметка
- **CSS3** — стилизация с поддержкой темной/светлой темы

## 📦 Требования

Перед установкой убедитесь, что у вас установлены:

- **Python 3.7 или выше**
  - Проверка: `python --version` или `python3 --version`
  - Скачать: [python.org](https://www.python.org/downloads/)

- **pip** (обычно устанавливается вместе с Python)
  - Проверка: `pip --version` или `pip3 --version`

- **Веб-браузер** (Chrome, Firefox, Edge, Safari)

## 🚀 Установка

### Шаг 1: Клонирование или скачивание проекта

Если проект находится в репозитории:
```bash
git clone <url-репозитория>
cd "project  копия"
```

Или просто откройте папку проекта в терминале.

### Шаг 2: Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 3: Установка зависимостей

Убедитесь, что виртуальное окружение активировано, затем:

```bash
pip install flask flask-cors
```

Или создайте файл `requirements.txt` в корне проекта:

```txt
Flask==3.0.0
flask-cors==4.0.0
```

И установите зависимости:
```bash
pip install -r requirements.txt
```

## ▶️ Запуск

### Шаг 1: Запуск Backend сервера

1. Откройте терминал в папке проекта
2. Перейдите в папку `backend`:
   ```bash
   cd backend
   ```

3. Запустите Flask-приложение:
   ```bash
   python app.py
   ```
   
   Или на macOS/Linux:
   ```bash
   python3 app.py
   ```

4. Вы должны увидеть сообщение:
   ```
   ✅ База данных инициализирована.
   * Running on http://0.0.0.0:5000
   * Running on http://127.0.0.1:5000
   ```

5. **Не закрывайте этот терминал!** Сервер должен работать постоянно.

### Шаг 2: Открытие Frontend

1. Откройте **новый терминал** (не закрывая первый!)
2. Перейдите в папку `frontend`:
   ```bash
   cd frontend
   ```

3. Запустите локальный веб-сервер:

   **Вариант A: Python (самый простой)**
   ```bash
   # Python 3
   python -m http.server 8000
   
   # Или Python 2
   python -m SimpleHTTPServer 8000
   ```

   **Вариант B: Node.js (если установлен)**
   ```bash
   npx http-server -p 8000
   ```

   **Вариант C: PHP (если установлен)**
   ```bash
   php -S localhost:8000
   ```

4. Откройте браузер и перейдите по адресу:
   ```
   http://localhost:8000
   ```

### Альтернативный способ (без локального сервера)

Если у вас нет возможности запустить локальный сервер, вы можете открыть `frontend/index.html` напрямую в браузере, но при этом могут возникнуть проблемы с CORS. Рекомендуется использовать локальный сервер.

## 📖 Использование

### Индивидуальное планирование

1. Выберите сценарий **"🎯 Индивидуальный"**
2. Используйте поиск и фильтры для нахождения интересующих событий
3. Кликайте на карточки событий, чтобы выбрать их (выбранные подсвечиваются)
4. Следите за счетчиком выбранных событий
5. Нажмите **"✨ Сгенерировать маршруты"**
6. Просмотрите предложенные маршруты
7. Нажмите **"📋 Копировать маршрут"**, чтобы скопировать его в буфер обмена

### Групповое планирование

1. Выберите сценарий **"👥 Групповой"**
2. Добавьте участников группы, введя имя и нажав **"+"**
3. Кликните на участника, чтобы сделать его активным
4. Выберите события для активного участника
5. Повторите для всех участников
6. Нажмите **"🤝 Найти общий маршрут"**
7. Система найдет события, которые подходят всем участникам

### Корректировка на лету

1. Выберите сценарий **"🔄 Корректировка"**
2. Создайте начальный маршрут, выбрав события и нажав **"✨ Создать начальный маршрут"**
3. Просмотрите текущий маршрут
4. Выберите новые события для добавления
5. Нажмите **"🔄 Перестроить маршрут"** для автоматического добавления
6. Используйте **"🔄 Сбросить маршрут"**, чтобы вернуться к исходному состоянию

### Дополнительные функции

- **Поиск**: Введите название события в поле поиска
- **Фильтры по времени**: Нажмите на кнопки "🌅 Утро", "☀️ День", "🌆 Вечер"
- **Подсветка ближайших событий**: События, которые начнутся в течение часа, автоматически подсвечиваются
- **Темная/светлая тема**: Нажмите на кнопку 🌙/☀️ в правом верхнем углу

## 📁 Структура проекта

```
project  копия/
│
├── backend/
│   ├── app.py              # Flask-приложение и API
│   └── events.db           # База данных SQLite (создается автоматически)
│
├── frontend/
│   ├── index.html          # Главная страница
│   ├── script.js           # JavaScript логика
│   └── style.css           # Стили
│
└── README.md               # Этот файл
```

### API Endpoints

- `GET /api/events` — получить список всех событий
- `POST /api/generate-routes` — сгенерировать маршруты
  - Body: `{ "selectedEventIds": [1, 2, 3] }`
  - Или для группы: `{ "groupPreferences": [{ "selectedEventIds": [1, 2] }] }`
  - Или для корректировки: `{ "selectedEventIds": [4], "currentRoute": [...] }`

## 🔧 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'flask'"

**Решение:**
- Убедитесь, что виртуальное окружение активировано
- Установите зависимости: `pip install flask flask-cors`
- Проверьте, что используете правильную версию Python: `python --version`

### Проблема: "Address already in use" при запуске Flask

**Решение:**
- Порт 5000 уже занят другим приложением
- Остановите другое приложение или измените порт в `app.py`:
  ```python
  app.run(debug=True, host='0.0.0.0', port=5001)  # Измените 5000 на 5001
  ```
- Не забудьте также изменить URL в `frontend/script.js`:
  ```javascript
  const response = await fetch('http://localhost:5001/api/events');
  ```

### Проблема: CORS ошибки в браузере

**Решение:**
- Убедитесь, что Flask-сервер запущен
- Проверьте, что используете локальный веб-сервер для frontend (не открываете файл напрямую)
- Убедитесь, что `flask-cors` установлен: `pip install flask-cors`

### Проблема: События не загружаются

**Решение:**
- Проверьте, что backend-сервер запущен и работает
- Откройте в браузере `http://localhost:5000/api/events` — должны увидеть JSON с событиями
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что в `script.js` правильный URL: `http://localhost:5000`

### Проблема: База данных не создается

**Решение:**
- Убедитесь, что у вас есть права на запись в папку `backend`
- Проверьте, что SQLite установлен (обычно входит в Python)
- Удалите файл `backend/events.db`, если он существует, и перезапустите сервер

### Проблема: Фильтры и поиск не работают

**Решение:**
- Откройте консоль браузера (F12) и проверьте на наличие ошибок JavaScript
- Убедитесь, что все файлы загружены правильно
- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: Кнопка "Копировать маршрут" не работает

**Решение:**
- Функция копирования требует HTTPS или localhost
- Убедитесь, что открываете приложение через `http://localhost:8000`, а не через `file://`
- Некоторые браузеры могут блокировать доступ к буферу обмена — разрешите доступ при запросе

## 🎨 Настройка

### Изменение порта Backend

В файле `backend/app.py` измените последнюю строку:
```python
app.run(debug=True, host='0.0.0.0', port=5000)  # Измените 5000 на нужный порт
```

И в `frontend/script.js` измените все вхождения `localhost:5000` на `localhost:НОВЫЙ_ПОРТ`.

### Добавление своих событий

События хранятся в базе данных SQLite. Вы можете:
1. Отредактировать функцию `init_db()` в `backend/app.py`
2. Или добавить события через SQL-запросы
3. Или создать API endpoint для добавления событий

### Изменение темы

Тема сохраняется в `localStorage` браузера. Чтобы сбросить:
1. Откройте консоль браузера (F12)
2. Выполните: `localStorage.removeItem('theme')`
3. Обновите страницу

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Убедитесь, что все зависимости установлены
3. Проверьте консоль браузера (F12) и терминал на наличие ошибок

## 🔗 Подключение к GitHub

### Быстрая настройка (рекомендуется)

**Windows:**
```bash
setup-git.bat
```

**macOS/Linux:**
```bash
chmod +x setup-git.sh
./setup-git.sh
```

Скрипт автоматически выполнит инициализацию Git, добавит файлы и создаст первый коммит.

### Ручная настройка

#### Шаг 1: Инициализация Git репозитория

Откройте терминал в корневой папке проекта и выполните:

```bash
git init
```

#### Шаг 2: Добавление файлов

```bash
git add .
```

#### Шаг 3: Первый коммит

```bash
git commit -m "Initial commit: RouteFest project"
```

#### Шаг 4: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Введите название репозитория (например, `routefest`)
4. **Не** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите **"Create repository"**

#### Шаг 5: Подключение к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды (замените `YOUR_USERNAME` и `YOUR_REPO_NAME` на свои):

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/username/routefest.git
git branch -M main
git push -u origin main
```

#### Шаг 6: Последующие коммиты

После внесения изменений:

```bash
# Проверить изменения
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

### Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть удаленные репозитории
git remote -v

# Скачать изменения с GitHub
git pull

# Создать новую ветку
git checkout -b feature-name

# Переключиться на ветку
git checkout main
```

### Если возникли проблемы

**Ошибка: "remote origin already exists"**
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

**Ошибка: "failed to push some refs"**
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

**Ошибка аутентификации**
- Используйте Personal Access Token вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- Или используйте GitHub CLI: `gh auth login`

---

**Приятного использования RouteFest! 🎪✨**

